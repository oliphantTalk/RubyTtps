<p id="notice"><%= notice %></p>
<table class="table table-hover table-bordered">
  <thead >
  <tr>
    <th>Nombre</th>
    <th>Año(Plan)</th>
    <th>Descripcion</th>
    <th>Cursos</th>
  </tr>
  </thead>

  <tbody>
  <tr>
    <td><%= @subject.name %></td>
    <td><%= @subject.year %></td>
    <td><%= @subject.detail %></td>

    <td><table><% @subject.courses.each do |course| %>
        <tr><td><%= link_to course.year, course_path %></td></tr>
        <% end %>
        </table></td>


  </td>


  </tr>
  </tbody>
</table>
<div class="btn-group btn-group-sm">
  <%= link_to 'Edit', edit_subject_path(@subject) %>
  <%= link_to 'Back', subjects_path %>
</div>
